# FIX REPORT 094 — Workflow, Mobile Roles, and Cross-Panel Binding

## الهدف
ربط تطبيق الموظف ولوحة HR ولوحة المدير التنفيذي ولوحة التقني/السكرتير التنفيذي ببعضها، وتحويل الصفحات الجديدة من واجهات ثابتة إلى مراكز تشغيل تقرأ بيانات Supabase قدر الإمكان.

## أهم التعديلات
- توحيد تطبيق الموظف: كل الموظفين يدخلون من نفس التطبيق، وتظهر إضافات المدير أو لجنة الخلافات حسب الدور/الفريق.
- إضافة مركز "إدارة فريقي" داخل تطبيق الموظف للمديرين.
- إضافة مركز "لجنة الخلافات" داخل تطبيق الموظف لأعضاء اللجنة.
- إضافة صفحة HR: "مركز الربط والتشغيل".
- إضافة صفحة HR: "ربط الموبايل والصلاحيات".
- ربط المراكز الجديدة ببيانات: الموظفين، المستخدمين، الإشعارات، Push subscriptions، طلبات الموقع، الشكاوى، الإجازات، المأموريات، وتقييمات KPI عند توفرها.
- تحسين التنسيقات والألوان لكروت HR والموبايل.
- تحديث Service Worker/cache/version إلى v31-production-hardening-098.
- تحديث expectedPatch إلى 098_location_security_edge_hardening.

## الحفاظ على الملفات
- لم يتم حذف `.git` من نسخة العمل.
- لم يتم حذف `supabase/.temp`.
- لم يتم حذف ملفات الإعدادات المحلية.
- حزمة الرفع العامة مخصصة للنشر الاستاتيكي ولا تشمل ملفات Git أو أسرار.

## Supabase
- Migration جديد: `supabase/migrations/20260508193000_098_location_security_edge_hardening.sql`
- تحديث `supabase/sql/RUN_IN_SUPABASE_SQL_EDITOR.sql`
- تحديث `supabase/sql/VERIFY_AFTER_SUPABASE_DEPLOY.sql`

## الفحوصات
تم تشغيل فحوصات npm والـ SQL والـ Release artifact بعد التعديل، ويتم توثيق نتائجها في الرد النهائي.
