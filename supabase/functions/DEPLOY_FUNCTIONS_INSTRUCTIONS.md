# Edge Functions - V29 Clean

الوظائف النشطة التي يتم نشرها الآن:

```text
admin-create-user
admin-update-user
resolve-login-identifier
passkey-register
send-attendance-reminders
send-push-notifications
```

تم توحيد إرسال الإشعارات في:

```text
send-push-notifications
```

الوظيفة القديمة `send-push-notifications` تم نقلها إلى:

```text
_archive/deprecated-functions/send-push-notifications
```

لا تنشرها مرة أخرى إلا كحل رجوع مؤقت.

ملاحظة: `employee-register` تم تعطيلها وأرشفتها لأن التسجيل من لوحة HR فقط.
